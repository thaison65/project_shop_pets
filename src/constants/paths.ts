type PathType = {
  [key: string]: string;
};

const instance: PathType = {
  default: '/',
  home: 'home',
  category: 'category',
  about: 'about',
  contact: 'contact',
};

export default instance;
